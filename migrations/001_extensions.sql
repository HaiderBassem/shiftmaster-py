-- =====================================================
-- Extensions
-- =====================================================

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";      -- For generating UUIDs
CREATE EXTENSION IF NOT EXISTS "pgcrypto";       -- For encryption and advanced functions
-- CREATE EXTENSION IF NOT EXISTS "timescaledb";  -- Optional for large time-series data (enable if using TimescaleDB)
