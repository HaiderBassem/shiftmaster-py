apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications
  namespace: shiftmaster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications
  template:
    metadata:
      labels:
        app: notifications
    spec:
      containers:
      - name: notifications
        image: ghcr.io/haiderbassem/shiftmaster-py-notifications:latest
        imagePullPolicy: Always
        env:
        - name: DB_HOST
          value: "prod-db"
        - name: REDIS_URL
          value: "redis://prod-redis:6379/0"
        - name: RABBITMQ_URL
          value: "amqp://shiftadmin:shiftpass@prod-rabbitmq:5672/"
        - name: DB_PASSWORD
          value: "supersecretpassword"
        - name: DB_USER
          value: "shift_py"
        - name: DB_NAME
          value: "shift_master_py_db"
        - name: JWT_SECRET
          value: "supersecretjwtkeythatislongenough32chars"
        - name: AUTH_SERVICE_URL
          value: "http://prod-auth:8001"
        - name: SCHEDULE_SERVICE_URL
          value: "http://prod-schedule:8002"
        - name: NOTIFICATION_SERVICE_URL
          value: "http://prod-notifications:8003"
        - name: MONOLITH_URL
          value: "http://prod-monolith:8004"
        ports:
        - containerPort: 8003
